

INSTALL

Installing this module should be trivial since it requires no
dependencies:

  $ perl Makefile.PL
  $ make
  $ make test
  $ make install

Once installed, or even before, you should be able to run
bin/bigrams.pl to see how it works.

-- 
Eric Lease Morgan <eric_morgan@infomotions.com>
June 19, 2009
